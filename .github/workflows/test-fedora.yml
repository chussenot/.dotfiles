name: Fedora

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  install:
    name: Install (Fedora 41)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Build test image
        run: |
          docker build \
            -f .tests/docker/Fedora-41.Dockerfile \
            -t dotfiles-test-fedora .

      - name: Verify installation
        run: |
          docker run --rm dotfiles-test-fedora \
            /home/dev/.dotfiles/.tests/check.sh

      - name: Upload install log on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: install-log-fedora
          path: /tmp/docker-build-*.log
          retention-days: 7
