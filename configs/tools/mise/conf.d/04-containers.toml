# Container & Kubernetes tools
[tools]

# --- Core container tooling ---
ctop = "0.7.7"
podman = "5.8.2"

# --- Kubernetes core ---
helm = "4.2.0"
k9s = "0.50.18"
kubectl = "1.36.1"
kind = "0.31.0"
krew = "0.5.0"
kustomize = "5.8.1"
# cilium-cli = "0.19.2"

# --- Kubernetes UX / debugging ---
stern = "1.34.0"

# --- Local dev & workflows ---
# skaffold = "2.17.1"
tilt = "0.37.3"

# --- Security / supply chain ---
# trivy = "0.69.2"
# cosign = "3.0.5"

# --- Registry & image inspection ---
crane = "0.21.6"

# --- Other ---
"go:github.com/jesseduffield/lazydocker" = "0.25.2"
