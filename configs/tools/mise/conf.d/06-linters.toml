# Linters, formatters, and code quality tools
[tools]
prettier = "3.8.1"

# Python linters & formatters (via pipx)
"pipx:check-jsonschema" = "0.37.0"
"pipx:mypy" = "1.19.1"
"pipx:ruff" = "0.15.6"
"pipx:pytest" = "9.0.2"
