# Global Claude Code instructions

Personal, machine-wide guidance for Claude Code. Managed in dotfiles
(`configs/tools/claude/CLAUDE.md`) and symlinked to `~/.claude/CLAUDE.md`.

Add global preferences here. Project-specific instructions belong in each
project's own `CLAUDE.md` (e.g. this repo's root `CLAUDE.md`), which takes
precedence over this file.
