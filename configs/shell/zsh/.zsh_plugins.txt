# Antidote plugins file
# Format: repo/name [kind:type] [branch:name] [path:path] [deps:repo1 repo2]
# See https://antidote.sh/usage for more information

# Completions (fpath-based for proper completion loading)
zsh-users/zsh-completions path:src kind:fpath

# History substring search (load early for keybindings)
zsh-users/zsh-history-substring-search

# Syntax highlighting (load after history-substring-search)
zdharma-continuum/fast-syntax-highlighting

# Git tools
paulirish/git-open
